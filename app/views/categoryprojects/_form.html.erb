<%= form_for(categoryproject) do |f| %>
  <% if categoryproject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(categoryproject.errors.count, "error") %> prohibited this categoryproject from being saved:</h2>

      <ul>
      <% categoryproject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :categoryprojectstring %>
    <%= f.text_field :categoryprojectstring %>
  </div>

  <div class="field">
    <%= f.label :categoryprojectorder %>
    <%= f.number_field :categoryprojectorder %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
